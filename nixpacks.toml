[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install --no-cache-dir sentence-transformers==2.7.0",
    "pip install --no-cache-dir -r requirements_optimized.txt"
]

[start]
cmd = "uvicorn optimized_app:app --host 0.0.0.0 --port "
